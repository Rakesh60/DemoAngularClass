import { SiInterestPipe } from './si-interest.pipe';

describe('SiInterestPipe', () => {
  it('create an instance', () => {
    const pipe = new SiInterestPipe();
    expect(pipe).toBeTruthy();
  });
});
